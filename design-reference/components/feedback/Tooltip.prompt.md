Hover/focus hint around a single trigger.

```jsx
<Tooltip content="Tải xuống PDF" side="bottom">
  <IconButton label="Tải"><i data-lucide="download" /></IconButton>
</Tooltip>
```

- **side**: top (default) / bottom / left / right.
