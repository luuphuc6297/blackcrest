Transient notification card. Place inside a fixed-position container (e.g. bottom-right).

```jsx
<Toast
  tone="success"
  icon={<i data-lucide="check-circle-2" />}
  title="Đã phát hành báo cáo"
  message="Báo cáo Quý III đã gửi tới 248 nhà đầu tư."
  onClose={dismiss}
/>
```
