Checkbox with the violet accent fill. Works controlled or uncontrolled.

```jsx
<Checkbox label="Ghi nhớ đăng nhập" defaultChecked />
<Checkbox label="Tôi đồng ý với điều khoản" checked={agree} onChange={e => setAgree(e.target.checked)} />
```
